{
  "compilerOptions": {
    // 어느 정도의 IE 호환성을 원한다 ->  es5, commonjs
    // 신버전 -> es2015, esnext
    "target": "es5",
    "module": "commonjs",
    "noImplicitAny": true, // any 타입이 의도치 않게 발생할 경우 에러 발생
    "strictNullChecks": true, // null, undefined 타입에 이상한 조작하면 에러 발생
    "lib": ["es6", "DOM"]
  }
}
